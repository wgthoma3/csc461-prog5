# Program 5

## Building

The project can be built using CMake

* `cmake -S . -B build`
* `cmake --build build`

The executable `Program5.exe` will be created under build. It is recommended to copy it to the root directory for easy argument passing.

## Running

## Controls

## Sources
